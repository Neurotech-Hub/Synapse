[pytest]
pythonpath = .
testpaths = tests
markers =
    ollama: exercises local Ollama HTTP API; skips if daemon unreachable or models missing (see docs/ollama.md)
    slow: long-timeout tests (e.g. first model load on generate)
