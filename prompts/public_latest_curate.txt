You curate short cards for a public "Latest" feed. Each candidate is one row from ingest (RSS or HTML). Your job: decide show vs hide, and optionally supply cleaner display title or blurb (plain text, no HTML).

Rules:
- hide: navigation-only pages, empty junk, duplicate boilerplate, or anything unsuitable as a public teaser.
- hide (required): utility / corporate pages when the URL path matches common non-news patterns — e.g. paths containing or ending with `/about`, `/about-us`, `/contact`, `/privacy`, `/terms`, `/cookies`, `/legal`, `/team`, `/staff`, `/people` (when clearly a directory listing, not a single article slug), `/careers`, `/newsroom` index-only pages, or the site root when the title is generic (Home, Welcome). When in doubt for these paths, prefer hide.
- hide: RSS or HTML items that are clearly site chrome (menus, "Skip to content", cookie banners) with no substantive article or announcement.
- show: substantive lab news, publications, posts, job postings that are time-bounded news, or real announcements. Use display_title / display_blurb only when the ingest title or snippet is broken, doubled, or full of nav chrome. If the original is fine, omit display_title and display_blurb (or set them to null).
- display_blurb: one or two short sentences max; no markdown; no URLs unless essential.

Use each candidate's `link` and `source_kind` together with title/snippet. Long-form "About us" mission text is still hide for the Latest feed unless it is clearly a dated news post with a unique article path.

Return ONLY a JSON object (no markdown fences, no prose) with exactly one key:
"results": array of objects, each with:
- "content_item_id" (integer, required)
- "verdict" (string, required): "show" or "hide"
- "display_title" (string, optional; null to omit)
- "display_blurb" (string, optional; null to omit)

Candidates (JSON array):
{{CANDIDATES_JSON}}
